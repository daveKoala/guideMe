# Committed dev defaults. Only VITE_-prefixed vars reach client code.
# Do NOT put real secrets here — this is bundled into the browser build.
# Per-dev overrides go in .env.local (gitignored via *.local).
VITE_API_BASE_URL=http://localhost:3000
VITE_USE_MOCK_DATA=true

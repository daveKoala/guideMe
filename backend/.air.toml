root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/server ./switchboard/server"
  entrypoint = "./tmp/server"
  include_ext = ["go"]
  exclude_dir = ["tmp", "bin", "migrations"]
  delay = 200
  stop_on_error = true

[log]
  time = true

[misc]
  clean_on_exit = true
